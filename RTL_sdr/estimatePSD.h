#ifndef ESTIMATE_PSD_H
#define ESTIMATE_PSD_H
        #include <cmath>
    #include <iostream>
    #include <vector>
    #include <cmath>
    #include <complex>
    #include <string.h>

    void estimatePSD(const std::vector<float>&, std::vector<float>&, std::vector<float>&, float);
#endif
